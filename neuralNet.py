import numpy as np

a b= 10
b = 10
c = 10

layer1 = np.zeros(a)
bias1 = 0
layer2 = np.zeros(b)
bias2 = 0
layer3 = np.zeros(c)
bias3 = 0

connections1t2 = np.zeros((a, b))
connections2t3 = np.zeros((b, c))

localMinimum(fx, x):
    for i < 10:
        dy = 
        dx = 0.1 
        if fx(x+dx) < fx(x):
                        
        else fx(x-dx) > fx(x):

localMinimum(lambda fx: fx^2, 2)
